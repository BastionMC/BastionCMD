:: The purpose of this file is to test the program/have it being
:: linked as a shortcut to easily start the program. This file
:: is the one being generated as a shortcut by the installer.

:: In-dev comment: should we automatically install Python with
:: the installer? Leave your answers here as comments.

:: Start the main.py file.
@echo off
python main.py

:: Ending loop so the window does not close after the program
:: is done running. Maybe not the most performant option.
:end
goto end